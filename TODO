Tests (React testing library)
    buttons
SPA (route)
Change js to ts

title (fixed)
navbar (fixed)
    home, cart (shows how many items, 99+)
Home
    4 items (name, img, price, addtocart button)

cart
    4 card (name, amt input field, increment/decrement)
    fake checkout button
    calculate total on right part